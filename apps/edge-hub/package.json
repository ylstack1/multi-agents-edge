{
  "name": "@midas/edge-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.0",
    "@midas/ai-provider": "workspace:*",
    "@midas/compiler": "workspace:*",
    "@midas/contracts": "workspace:*",
    "@midas/mcp-edge": "workspace:*",
    "@midas/vfs": "workspace:*",
    "hono": "^4.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
